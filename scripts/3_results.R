library(terra)
res <- terra::rast("scripts/2_omniscape_julia/test_area.tif")
currmap <- terra::rast("scripts/2_omniscape_julia/output/example/cum_currmap.tif")
norm <- terra::rast("scripts/2_omniscape_julia/output/example/normalized_cum_currmap.tif")
par(mfrow = c(1, 3))
plot(res, main = "Original resistance, test area")
plot(currmap, main = "Cumulative current map")
plot(norm, main = "Normalized cumulative current map")

png("figs/example.png", width = 550, height = 400)
par(mfrow = c(1, 3))
plot(res, main = "Original resistance, test area")
plot(currmap, main = "Cumulative current map")
plot(norm, main = "Normalized cumulative current map")
dev.off()
